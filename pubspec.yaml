name: telugu_cooking_app
description: A cooking app with Telugu language and voice support
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_bloc: ^9.1.1       # was 8.1.3 — v9 is current stable
  equatable: ^2.0.5

  # Speech & Voice
  speech_to_text: ^7.0.0     # was 6.6.0 — v1 embedding fix
  flutter_tts: ^4.2.2        # was 3.8.5 — v1 embedding fix

  # UI Components
  cached_network_image: ^3.4.1  # was 3.3.0
  shimmer: ^3.0.0
  flutter_staggered_grid_view: ^0.7.0
  lottie: ^3.3.1              # was 2.7.0
  google_fonts: ^6.2.1        # was 4.0.4 — const map Dart3 fix

  # Utilities
  shared_preferences: ^2.3.0  # was 2.2.2
  intl: ^0.20.2               # OK — matches flutter_localizations in Flutter 3.27+
  dio: ^5.7.0                 # was 5.4.0
  connectivity_plus: ^6.0.0   # was 5.0.2 — AGP8 fix
  share_plus: ^7.2.1         # was 7.2.1 — v1 embedding + new SharePlus API
  url_launcher: ^6.3.0        # was 6.2.2
  permission_handler: ^11.3.1 # was 10.4.5 — v1 embedding fix
  http: ^1.2.0
  path_provider: ^2.1.1  # temp directory for image export


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0      # was 3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/lottie/    # ← must include this folder
    - assets/images/
    - assets/fonts/

## 🎬 Animation Sequence (~3.2s total)
# 0ms    → Orange gradient background expands + subtle scale
# 200ms  → Lottie cooking animation fades + springs in
# 300ms  → Lottie starts playing (duration auto-matched to your file)
# 700ms  → "Ruchi / రుచి" title slides up
# 950ms  → Telugu tagline slides up
# 1200ms → "Made for Telugu food lovers" badge fades in
# 1300ms → Pulsating dots appear
# 3200ms → Fades + slides into HomeScreen